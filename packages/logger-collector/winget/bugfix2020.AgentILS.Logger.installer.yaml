# Winget installer manifest
# Submit to microsoft/winget-pkgs via PR after each release
# Replace <VERSION> with the actual release version
# Replace <SHA256> with the SHA256 of the Windows zip archive
PackageIdentifier: bugfix2020.AgentILS.Logger
PackageVersion: <VERSION>
Platform:
    - Windows.Desktop
MinimumOSVersion: 10.0.0.0
InstallerType: zip
Installers:
    - Architecture: x64
      InstallerUrl: https://github.com/bugfix2020/AgentILS/releases/download/%40agent-ils%2Flogger%40<VERSION>/agent-ils-logger-windows-amd64.zip
      InstallerSha256: <SHA256>
      NestedInstallerType: portable
      NestedInstallerFiles:
          - RelativeFilePath: agent-ils-logger.exe
            PortableCommandAlias: agent-ils-logger
ManifestType: installer
ManifestVersion: 1.6.0
