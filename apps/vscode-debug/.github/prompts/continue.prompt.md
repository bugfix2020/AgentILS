# /continue

1. Read `taskcard_get`, `handoff_get`, `control_mode_get`, and `task_summary_get`.
2. Continue from the recorded `currentStep`.
3. Keep the current task boundary; do not create a new task unless explicitly requested.
4. Reuse the task summary document as the only inherited cross-task memory when applicable.
