---
---

Docs-only refresh in `packages/logger/{README.md, README.zh-CN.md, LLM_USAGE.md}`:
replaced stale `run_task_loop.next` / `run_task` examples with real V1 events
(`interaction.submitted`, `request_user_clarification`). No API or runtime
behavior change — no version bump required.
